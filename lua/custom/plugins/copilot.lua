return {
  'zbirenbaum/copilot.lua',
  opts = {
    suggestion = {
      enabled = true,
      auto_trigger = true, -- can also trigger by pressing next or prev
      --keymap = { accept = '<C-a>', next = '<Right>', prev = '<Left>' },
    },
    panel = { enabled = false },
  },
}
